# MagScale 🚀

**MagScale** — це простий та ефективний інструмент для покращення якості відео (AI Upscale) за допомогою нейромережі `waifu2x-converter-cpp` та енкодера `ffmpeg`.

Скрипт автоматично розбиває відео на кадри, апскейлить їх за допомогою GPU, а потім збирає назад у відеофайл з оптимізацією розміру.

## ✨ Особливості
* **Автоматизація:** Одна команда для повного циклу обробки.
* **AI Upscale:** Використовує `waifu2x` (noise-scale level 1, x2 zoom).
* **Візуалізація:** Гарний прогрес-бар з розрахунком часу (ETA).
* **Чистота:** Автоматичне видалення тимчасових файлів.
* **NixOS First:** Повна підтримка Nix Flakes для легкої установки.

---

## ❄️ Встановлення на NixOS (Рекомендовано)

Завдяки Nix Flakes, ви можете запустити MagScale без встановлення або додати його у свою систему.

### Варіант 1: Одноразовий запуск
Якщо ви хочете просто обробити файл, не встановлюючи програму в систему:

    nix run github:OleksandrCEO/MagScale -- input_video.mp4

### Варіант 2: Встановлення через `configuration.nix`
Додайте цей репозиторій до ваших `inputs` та встановіть пакет:

1. У `flake.nix`:

    inputs.magscale.url = "github:OleksandrCEO/MagScale";

2. У вашій конфігурації (наприклад, `configuration.nix`):

    environment.systemPackages = [
        inputs.magscale.packages.${pkgs.system}.default
    ];

---

## 🐧 Встановлення на інші Linux (Ubuntu, Arch, Fedora)

Для звичайних дистрибутивів використовуйте універсальний Bash-скрипт.

### 1. Завантаження скрипта

    # Завантажити
    wget [https://raw.githubusercontent.com/OleksandrCEO/MagScale/main/magscale.sh](https://raw.githubusercontent.com/OleksandrCEO/MagScale/main/magscale.sh)

    # Зробити виконуваним
    chmod +x magscale.sh

    # Перемістити у bin (опціонально)
    sudo mv magscale.sh /usr/local/bin/magscale

### 2. Залежності
Переконайтеся, що у вас встановлені необхідні програми.

**Arch Linux / Manjaro:**

    sudo pacman -S ffmpeg waifu2x-converter-cpp libnotify

**Ubuntu / Debian:**

    sudo apt install ffmpeg libnotify-bin
    # Увага: waifu2x-converter-cpp потрібно зібрати самостійно 
    # або завантажити готовий бінарник з офіційного репозиторію.

---

## 🎬 Використання

Просто передайте шлях до відеофайлу:

    magscale video.mp4

Результат буде збережено поруч із оригіналом з суфіксом `_upscaled.mp4`.

**Приклад роботи:**
1. Витягування кадрів (10 fps для анімації).
2. AI обробка (використовує всі ядра CPU/GPU).
3. Склейка відео (x264, crf 18).

---

## 🛠 Технічні деталі
* **FPS:** За замовчуванням 10 (оптимально для анімації/стоп-моушн).
* **Алгоритм:** waifu2x-converter-cpp (noise-scale).
* **Формат виводу:** MP4 (H.264).